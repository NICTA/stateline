#!/usr/bin/env python

from distutils.core import setup, Extension

setup(name='stateline',
      version='0.1',
      description='Distributed Markov Chain Monte Carlo sampling.',
      packages=['stateline'])
